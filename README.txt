To run:
python3 alter.py

To test small files choose either:
example.csv with 4 as the decision
example2.csv with 4 as the decision

To test large file:(Takes around ~40 min)
googlesheets.csv with 5 as the attribute